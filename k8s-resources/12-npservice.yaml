apiVersion: v1
kind: Service
metadata:
  name: np-demo
spec:
  type: NodePort
  selector:
    name: roboshop
    course: devops
    purpose: np-demo
  ports:
  - protocol: TCP
    port: 80 # service port
    targetPort: 80 # container port

  